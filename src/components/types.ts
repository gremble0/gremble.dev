// TODO: Differentiate between these two types or merge them into one
export type SocialProps = {
    socialName: string;
    imageSrc: string;
    url: string;
}

export type ProjectProps = {
    projectName: string;
    imageSrc: string,
    url: string;
}
